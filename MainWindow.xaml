<Window x:Class="JustAnotherCalendarWidget.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CalendarWidget" Height="700" Width="1000"
        Background="Black" WindowStyle="None" ShowInTaskbar="False" ResizeMode="NoResize">
    <StackPanel MouseLeftButtonDown="StackPanel_MouseLeftButtonDown">
        <TextBlock x:Name="YearText" FontSize="32" FontWeight="Bold" Foreground="White" HorizontalAlignment="Center" Margin="0,5,0,5"/>
        <Grid x:Name="MonthsGrid" ClipToBounds="False">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
        </Grid>
    </StackPanel>
</Window>